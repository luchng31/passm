[package]
name = "passm-app"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

# Placeholder crate for the Tauri 2 desktop app. NO tauri/tauri-build deps yet —
# T11 adds the real Tauri setup (requires webkit2gtk system libs).

[dependencies]